/**
 * Typography — re-exported from design tokens for backward compatibility.
 */
import { fonts } from './designTokens';

export const typography = {
  h1: fonts.screenTitle,
  h2: fonts.sectionHeader,
  body: fonts.body,
  bodySmall: fonts.caption,
  button: fonts.button,
};
