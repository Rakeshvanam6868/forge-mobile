/**
 * AuthButton — backward compatible re-export of PrimaryButton.
 * All existing screens using AuthButton continue to work unchanged.
 */
export { PrimaryButton as AuthButton } from '../../../core/components/PrimaryButton';
